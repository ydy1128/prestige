import React from 'react';
import { connect } from 'react-redux';


let style = {
};

class RootCommment extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    render() {

        return (
          <div>
          </div>
        );
    }
}

const mapStateToProps = (state) => {
    return { };
};

export default connect(mapStateToProps, undefined)(RootCommment);
